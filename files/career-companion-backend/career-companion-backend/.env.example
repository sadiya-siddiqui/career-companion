# Server
NODE_ENV=development
PORT=5000

# MongoDB Atlas connection string
# Replace <username>, <password>, <cluster-url>, and <db-name>
MONGO_URI=mongodb+srv://sadiya:Sadiya1234cluster0.tzewogj.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0


# JWT
JWT_SECRET=replace_this_with_a_long_random_secret_string
JWT_EXPIRES_IN=7d

# CORS - comma separated list of allowed origins (use * for all, not recommended in production)
CLIENT_ORIGIN=http://localhost:3000

# Rate limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100
